Написать программу, в которой будут выбираться данные и выводиться на экран.
Селекционный экран – spfli-carrid

Выборку произвести с помощью конструкции WITH

Структура таблицы: 
carrid, connid, fldate, price(sflight), currency(sflight), carrname (из scarr), agencynum(есть в sbook), name (из stravelag)
Избавиться от дублей (в самом селекте).

Вывести таблицу с помощью CL_SALV_TABLE. Добавить тулбар, чтобы были все стандартные функции (сортировки, форматы и тд). 
При выводе скрыть поле: Номер турагенства (agencynum).
